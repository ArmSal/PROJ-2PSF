{% extends 'base.html.twig' %}

{% block title %}Nouvel article{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap">
            <h1 class="m-0">Créer un nouvel article</h1>
            <div>
                <a href="{{ path('app_article_index') }}" class="text-decoration-none">
                    <i class="bi bi-arrow-left-circle fs-3 text-secondary" title="Retour à la liste"></i>
                </a>
            </div>
        </div>
        {{ include('admin/article/_form.html.twig') }}
    </div>
{% endblock %}
